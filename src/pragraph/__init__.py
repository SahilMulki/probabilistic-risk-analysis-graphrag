"""pragraph — knowledge-graph RAG over U.S. NRC Licensee Event Reports (LERs),
with a probabilistic risk layer and a graph-vs-vector comparison.

Installed as an editable package (`pip install -e .`) so every module imports
its siblings by name (`from .models import ...`) with no sys.path juggling.
"""
