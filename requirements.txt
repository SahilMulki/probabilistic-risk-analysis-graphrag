# Dependencies are declared in pyproject.toml. This file installs the pragraph
# package (editable) plus those pinned deps, so `pip install -r requirements.txt`
# and `pip install -e .` are equivalent.
-e .
